# personal-knowledge-base-skill — Agent 指引

> 本仓库是一个**通用的个人知识库组织 skill**。规范正文在 `SKILL.md`，细则在 `references/`，可复制骨架在 `assets/template/`。

## 你要做什么

当用户要求**建立或整理个人知识库**、**清空收件箱**、**归档证书/资料**、**基于知识库写作业**时：

1. 先读 `SKILL.md`（完整规范）
2. 需要细则时读 `references/conventions.md`
3. 需要建库时复制 `assets/template/` 到目标目录（或运行 `scripts/init.*`）

## 三条不可违背的原则

- 每个作业一个文件夹
- 每个入库的原始文件配一个同名/同组索引笔记
- 修改后更新 frontmatter 的 `updated` 与根 `INDEX.md`

隐私：敏感材料（证件、他人信息）**只进私有库或 gitignore 目录**，引用一律 `<已脱敏>`。
