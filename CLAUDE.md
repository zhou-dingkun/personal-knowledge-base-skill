# CLAUDE.md — 兼容入口

> 权威规范见 `SKILL.md`（细则 `references/conventions.md`）。本文件仅作指引，避免多份规范漂移。

## 本仓库是「个人知识库组织」skill

- 技能本体：`SKILL.md`
- 建库骨架：`assets/template/`（复制到目标目录即可）
- 初始化脚本：`scripts/init.ps1`（Windows）/ `scripts/init.sh`（macOS/Linux）

## 三条硬性规则

1. 每个作业一个文件夹（`05-作业/YYYYMMDD-课程名-作业名/`）
2. 每个入库的 PDF/图片配一个同名/同组索引笔记
3. 修改后更新 frontmatter 的 `updated` 与根 `INDEX.md`

隐私：敏感材料只进私有库或 gitignore 目录；引用一律 `<已脱敏>`。
