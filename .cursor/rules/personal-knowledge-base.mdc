---
description: 个人知识库组织规范（四区流转 + 索引强制 + 作业一目录一作业）
globs:
  - "**/*.md"
alwaysApply: false
---

# 个人知识库 · 组织规范

规范全文见 `SKILL.md`，细则见 `references/conventions.md`。

## 结构
- `01-收件箱` 只放未整理内容；归位后删除原文
- `02-资料库` 原始材料**只增不改**，每个文件配同名/同组 `.md` 索引笔记
- `03-笔记` 按主题分子目录；`04-项目` 一项目一目录
- `05-作业` **每个作业一个文件夹**：`README.md` + `题目/` + `解答.md`

## 每次改动
- 更新 frontmatter 的 `updated`
- 新增/移动内容时同步根 `INDEX.md`

## 红线
- 不编造，不确定处写 `> 待确认：…`
- 敏感材料（证件、他人姓名/学号/编号）不进公开仓库、不外发，引用用 `<已脱敏>`
