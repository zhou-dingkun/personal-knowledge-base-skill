# 复刻指令 · 个人知识库组织架构

> **怎么用**：把下面 `=== 复刻指令开始 ===` 与 `=== 复刻指令结束 ===` 之间的**全部内容**，一次性发给你自己的 AI agent（DSH / Claude Code / Cursor / Copilot / Codex / Cline / Windsurf …）。
> 它会按规范在你指定目录里复刻出**同款知识库骨架**（17 个文件，逐字一致）。
>
> **特点：完全离线自包含**——不依赖网络、不依赖本仓库、不依赖任何特定 agent。

---

=== 复刻指令开始 ===

## 你的任务

在**当前工作目录**（或用户指定的目录）下，完整复刻一个"个人知识库"骨架。

1. 创建下面列出的**全部目录与文件**，内容**逐字照抄**，不要改写、不要精简、不要"优化"
2. 若同名文件已存在：**保留原内容**并报告冲突，除非用户明确要求覆盖
3. 创建完成后执行版本库初始化：
   ```bash
   git init -b main 2>/dev/null || git init
   git add -A
   git commit -m "chore: 初始化个人知识库骨架"
   ```
   （环境没有 git 就跳过，并告知用户）
4. 最后输出**完整目录树**与下面的自检清单结果

### 实现要求

- 用你的**文件写入**能力逐个建文件；若只有 shell，可用 `mkdir -p` + heredoc（`cat > 文件 <<'EOF'`），注意 heredoc 用**单引号 EOF** 以防变量被展开
- **文件内容必须逐字一致**，包括 YAML frontmatter 的 `---`、空行、缩进、标点、全角字符
- **特别注意以 `.` 开头的隐藏文件**：`.gitignore`、`.github/`、`.cursor/`、`.clinerules/` —— 很多环境默认不显示或容易漏建，务必逐个确认
- 中文文件名（`00-模板`、`01-收件箱` 等）要原样创建，不要转拼音/英文
- 不要额外创建本清单之外的文件

### 这个库的三条铁律（复刻后也要在库里保留）

1. **每个作业一个文件夹**（`05-作业/YYYYMMDD-课程名-作业名/`）
2. **每个入库的原始文件配一个同名/同组 `.md` 索引笔记**
3. **每次修改后更新 frontmatter 的 `updated` 与根 `INDEX.md`**

隐私红线：敏感材料（证件、他人姓名/学号/编号）不外发、不进公开仓库；引用一律 `<已脱敏>`。

### 目标结构

```text
<目标目录>/
├── AGENTS.md                          # 总规范（多数 AI 工具会话开始自动加载）
├── INDEX.md                           # 活索引（人机共同维护）
├── README.md                          # 人类说明 + Git 用法
├── .gitignore                         # 含 AGENTS.local.md 等私有覆盖层
├── CLAUDE.md                          # Claude Code 入口（指向 AGENTS.md）
├── .github/copilot-instructions.md    # GitHub Copilot 入口
├── .cursor/rules/knowledge-base.mdc   # Cursor 规则
├── .clinerules/knowledge-base.md      # Cline / Roo 规则
├── 00-模板/笔记模板.md
├── 01-收件箱/AGENTS.md
├── 02-资料库/AGENTS.md
├── 03-笔记/AGENTS.md
├── 04-项目/AGENTS.md
├── 05-作业/
│   ├── AGENTS.md
│   └── _模板/
│       ├── 作业README模板.md
│       └── 解答模板.md
└── 06-Agent工具/AGENTS.md
```

### 文件清单与内容（共 17 个）


#### 1. `AGENTS.md`

````text
# 个人知识库 — AI 工作指令

> 本文件是知识库的「宪法」。DSH / Claude Code / Cursor / Copilot / Codex / Cline 等 AI 工具在会话开始时**自动加载**。
> 各子目录另有 `AGENTS.md`，在处理该目录内容时自动加载，且**优先级更高**。

## 1. 这是什么

- 形态：纯 Markdown 文本 + 原始文件（PDF / 图片），Git 版本管理
- 内容：个人资料、证书、学业、研究、技能、生活记录，**默认不对外共享**
- 目标：可长期演进、可被任何 AI 工具读写、零厂商锁定

### 1.1 AI 工具兼容性（读与写）

- 本库规范对主流 AI 工具**通用**：DSH、Claude Code、Cursor、GitHub Copilot、Codex、Cline、Windsurf 等
- 兼容入口：`CLAUDE.md`、`.github/copilot-instructions.md`、`.cursor/rules/`、`.clinerules/` 均为指向本文件的指针，避免多份规范漂移
- 任何 AI 工具按本文件规则写入：新建笔记用 `00-模板/笔记模板.md`，修改后更新 frontmatter 的 `updated` 与 `INDEX.md`
- **唯一权威规范是本文件（AGENTS.md）**

## 2. 目录结构（四区 + 作业 + 工具 + 模板）

| 目录 | 用途 | 生命周期 |
|---|---|---|
| `01-收件箱` | 临时剪藏、随手笔记、灵感 | 定期清空归位 |
| `02-资料库` | 原始材料：证书 / PDF / 扫描件 / 图片，按主题分子目录 | 原始文件**只增不改** |
| `03-笔记` | 整理后的知识笔记，按主题分子目录 | 长期维护 |
| `04-项目` | 进行中的项目 / 课题 / 论文 | 完成后归档 |
| `05-作业` | AI 协助完成的作业，**每个作业一个文件夹** | 保留为记录 |
| `06-Agent工具` | 本机工具脚本、用法说明、环境说明 | 工具文档，长期维护 |
| `00-模板` | 笔记 / 作业模板 | 只读参考 |

流转模型：**收件箱 → 资料库 / 笔记 / 项目**。

## 3. 笔记规范（所有 Markdown 文件必须遵守）

### 3.1 Frontmatter（YAML 头部）

```yaml
---
type: note          # note | certificate | document | project | inbox | assignment | person
title: 笔记标题
tags: [学业, 证书]    # 受控词表，见 3.3
created: YYYY-MM-DD
updated: YYYY-MM-DD  # 每次修改必须更新
status: 活跃          # 收件箱 | 整理中 | 活跃 | 已完成 | 归档
---
```

### 3.2 命名

- 笔记类：`YYYYMMDD-简短描述.md`
- 资料类：`主题-描述.ext`
- 文件名避免空格、引号与 `\ / : * ? " < > |`

### 3.3 标签词表（受控，新增须在 INDEX.md 登记）

`学业` `科研` `证书` `技能` `编程` `生活` `健康` `财务` `项目` `灵感` `待办`

### 3.4 链接

- 内部链接优先用**相对路径**：`[标题](../03-笔记/学业研究/xxx.md)`
- Obsidian 的 `[[双链]]` 兼容但非权威形式

### 3.5 资料文件配套索引

每个入库的 PDF / 图片必须有**同名或同组的 `.md` 索引笔记**，写明：是什么、时间、来源、用途、存储路径。
**AI 优先读索引笔记，而不是直接解析二进制文件。**

## 4. 工作流（AI 执行规则）

### 4.1 收件箱清空
处理 `01-收件箱` 内容时：
1. 原始材料 → `02-资料库` 对应子目录 + 写索引笔记
2. 知识笔记 → `03-笔记` 按主题归类
3. 项目内容 → `04-项目`
4. 归位完成后**删除收件箱原文**，并在 `INDEX.md` 登记

### 4.2 新建笔记
用 `00-模板/笔记模板.md` 初始化 → 填 frontmatter → 更新 `INDEX.md`。

### 4.3 每次修改后
- 更新 frontmatter 的 `updated`
- 内容结构变化时同步更新 `INDEX.md`

### 4.4 作业（AI 依据知识库协助完成）
- **每个作业一个文件夹**：`05-作业/YYYYMMDD-课程名-作业名/`，完整规范见 `05-作业/AGENTS.md`
- 作答写入该文件夹的 `解答.md`，**标注知识库来源**、不编造，并同步更新 `README.md` 的 `status`
- 可复用知识点回灌到 `03-笔记/` 并互链

## 5. Git 规则

- 提交信息用**约定式提交**：`feat:` `fix:` `docs:` `chore:` `refactor:`
- 一个逻辑变更一个提交；提交前 `git status` 确认无敏感文件
- 禁止提交：密钥、密码、隐私证件照片、`AGENTS.local.md`
- 有远端时提交后 `git push`

## 6. 隐私与安全（红线）

- 本库可能含个人证件 / 证书，以及包含**他人**姓名 + 学号 + 编号的材料
- 敏感内容**绝不推送到公开仓库、绝不外发**，不向无关方透露他人信息
- 身份证号、手机号、银行账号等引用一律用占位符 `<已脱敏>`；AI 输出此类内容前必须提醒用户
- 需要纳管敏感材料时，用**私有**仓库，或把对应目录写入 `.gitignore`

## 7. 常用命令

```bash
git add -A
git status                 # 检查无敏感文件
git commit -m "docs: …"
git log --oneline -10
git push
```

## 8. 工具库与环境

本机可用的工具脚本、调用方法与环境清单统一放在 `06-Agent工具/`（该目录有独立 `AGENTS.md`，处理其中的内容时自动加载）：

| 文档 | 内容 |
|---|---|
| `06-Agent工具/环境说明.md` | 本机环境清单（Python / Node / 图像工具 / Git 等） |
| `06-Agent工具/工具用法.md` | 工具调用方法（PDF 文本提取、页面渲染、图像转换等） |
| `06-Agent工具/脚本/` | 可直接调用的脚本 |

原则：**能读 Markdown 就不解析二进制**；必须解析时，把结论沉淀成索引笔记，避免重复解析。
````

#### 2. `INDEX.md`

````text
# 知识库索引（INDEX）

> 由本人与 AI 共同维护的活索引。新增 / 移动 / 归档笔记时更新对应章节。
> 标签词表新增词条也在此登记。

## 02-资料库

（按主题分子目录；每个原始文件配同名索引笔记）

## 03-笔记

（按主题分子目录，如 学业研究 / 技能学习 / 生活记录）

## 04-项目

（暂无）

## 05-作业

（暂无；每个作业一个文件夹，命名 `YYYYMMDD-课程名-作业名`）

## 06-Agent工具

（本机工具脚本与用法说明）

## 01-收件箱

（待整理内容）

## 标签词表登记

`学业` `科研` `证书` `技能` `编程` `生活` `健康` `财务` `项目` `灵感` `待办`
````

#### 3. `README.md`

````text
# 个人知识库

纯 Markdown + 原始文件 + Git 的个人知识库。**AI 友好、零厂商锁定、可长期演进。**

## 目录结构（四区 + 作业 + 工具 + 模板）

| 目录 | 用途 | 生命周期 |
|---|---|---|
| `01-收件箱` | 临时剪藏、随手笔记、灵感（未整理） | 定期清空归位 |
| `02-资料库` | 原始材料：证书、PDF、扫描件、图片 | 原始文件只增不改 |
| `03-笔记` | 整理后的知识笔记，按主题分子目录 | 长期维护 |
| `04-项目` | 进行中的项目 / 课题 / 论文 | 完成后归档 |
| `05-作业` | AI 依据知识库协助完成的作业（**每个作业一个文件夹**） | 保留为记录 |
| `06-Agent工具` | 工具脚本、用法说明与本机环境说明 | 工具文档 |
| `00-模板` | 笔记 / 作业模板 | 只读参考 |

流转模型：**收件箱 → 资料库 / 笔记 / 项目**。详见根目录 `AGENTS.md`。

## AI 自动加载配置

| 文件 | 加载时机 | 说明 |
|---|---|---|
| `AGENTS.md`（根） | 多数 AI 工具会话开始自动加载 | 知识库总规范 |
| 各子目录 `AGENTS.md` | AI 处理该目录内容时自动加载 | 该区域专属规则，优先级更高 |
| `AGENTS.local.md` | 自动加载（本机） | 私有覆盖层，已 gitignore |
| `CLAUDE.md` / `.github/copilot-instructions.md` / `.cursor/rules/` / `.clinerules/` | 各工具各自读取 | 均指向 `AGENTS.md` 的指针 |

## Git 使用

```bash
git add -A
git status                     # 检查无敏感文件
git commit -m "docs: 添加笔记 xxx"
git log --oneline -10
git push
```

配置远端（可选）：

```bash
# 私有仓库（推荐，资料常含个人信息）
gh repo create my-knowledge-base --private --source=. --remote=origin --push
```

## 约定速览

- 每篇笔记带 YAML frontmatter（type / title / tags / created / updated / status）
- 命名：笔记 `YYYYMMDD-简短描述.md`；资料 `主题-描述.ext`
- 每个入库 PDF / 图片配一个同名 `.md` 索引笔记
- 内部链接优先用相对路径
- 改完更新 `updated` 与 `INDEX.md`
- **隐私红线**：敏感材料不进公开仓库、不外发

> 本库由 [personal-knowledge-base-skill](https://github.com/) 脚手架生成。
````

#### 4. `.gitignore`

````text
# ===== 本机私有 AI 指令覆盖层（自动加载，但不入库）=====
AGENTS.local.md
CLAUDE.local.md

# ===== 操作系统 =====
Thumbs.db
Desktop.ini
.DS_Store

# ===== Office / 文档临时文件 =====
~$*.docx
~$*.xlsx
~$*.pptx

# ===== Obsidian 本地工作区（布局与缓存为设备相关）=====
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# ===== 编辑器 =====
.vscode/
.idea/

# ===== 临时与日志 =====
*.log
tmp/

# ===== 隐私边界 =====
# 本库可能含个人证件 / 他人信息。红线：绝不推送到公开/共享仓库、绝不外发。
# 若选择"敏感材料不入库"，在此加入对应目录，例如：
# 02-资料库/个人证件/
````

#### 5. `CLAUDE.md`

````text
# CLAUDE.md — 兼容入口

> 权威规范见根目录 `AGENTS.md`（DSH / Claude Code / Cursor / Copilot 等自动加载）。本文件仅作指引，避免规范漂移。

## 请务必遵守 AGENTS.md 中的全部规则

- 目录结构：01-收件箱 / 02-资料库 / 03-笔记 / 04-项目 / 05-作业 / 06-Agent工具 / 00-模板
- 每篇笔记带 YAML frontmatter（type / title / tags / created / updated / status）
- 命名：`YYYYMMDD-简短描述.md`；每个入库 PDF / 图片配同名 `.md` 索引笔记
- 修改后更新 `updated` 与 `INDEX.md`
- Git 提交用约定式：`feat:` `fix:` `docs:` `chore:` `refactor:`
- 隐私红线：敏感材料不进公开仓库、不外发，引用用 `<已脱敏>`
- 处理子目录内容时，该目录下的 `AGENTS.md` 优先级更高
````

#### 6. `.github/copilot-instructions.md`

````text
# GitHub Copilot Instructions

> 权威规范见根目录 `AGENTS.md`（Copilot 亦自动加载）。本文件仅作兼容指引。

## 本知识库为个人 Markdown 知识库，请遵守 AGENTS.md 全部规则

- 结构：01-收件箱（临时）/ 02-资料库（原始材料 + 同名索引笔记）/ 03-笔记（主题笔记）/ 04-项目 / 05-作业（**每个作业一个文件夹**）/ 06-Agent工具 / 00-模板
- 新笔记：复制 `00-模板/笔记模板.md`，填 frontmatter（type/title/tags/created/updated/status）
- 命名：`YYYYMMDD-简短描述.md`；内部链接用相对路径
- 修改后：更新 frontmatter `updated` 与根 `INDEX.md`
- 隐私红线：不输出、不提交个人敏感信息（身份证号 / 手机号 / 银行账号等用 `<已脱敏>`）
````

#### 7. `.cursor/rules/knowledge-base.mdc`

````text
---
description: 个人知识库规范（四区流转 + 索引强制 + 一作业一目录）
globs:
  - "**/*.md"
alwaysApply: true
---

# 个人知识库规范

权威规范见根 `AGENTS.md`。

- `01-收件箱` 只放未整理内容；归位后删原文
- `02-资料库` 原始材料**只增不改**，每文件配同名 `.md` 索引笔记
- `03-笔记` 按主题分子目录；`04-项目` 一项目一目录
- `05-作业` **每个作业一个文件夹**：`README.md` + `题目/` + `解答.md`
- 每篇笔记带 frontmatter；改后更新 `updated` 与 `INDEX.md`
- 不编造，不确定处写 `> 待确认：…`
- 敏感材料不外发、不进公开仓库，引用用 `<已脱敏>`
````

#### 8. `.clinerules/knowledge-base.md`

````text
# 个人知识库规范（Cline / Roo）

权威规范见根 `AGENTS.md`。

- 四区流转：`01-收件箱` → `02-资料库` / `03-笔记` / `04-项目`
- `02-资料库` 原始材料只增不改，每个文件配同名 `.md` 索引笔记
- **每个作业一个文件夹**（`05-作业/YYYYMMDD-课程名-作业名/`）
- 每篇笔记带 frontmatter；改后更新 `updated` 与 `INDEX.md`
- 不编造，不确定处标 `> 待确认：…`
- 敏感材料不外发、不进公开仓库，引用用 `<已脱敏>`
````

#### 9. `00-模板/笔记模板.md`

````text
---
type: note
title: 
tags: []
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: 整理中
source: 
---

# 标题

## 背景

（为什么记这条？上下文是什么？）

## 要点

- 

## 关联

- [相关笔记](../03-笔记/主题/xxx.md)
````

#### 10. `01-收件箱/AGENTS.md`

````text
# 01-收件箱 — 临时内容区

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。

## 规则
1. 本目录只放**未整理的**临时笔记、剪藏、灵感、待办——不讲究格式，先存下来
2. AI 处理本目录内容时，按根 `AGENTS.md` 第 4.1 节归位：
   - 原始材料 → `02-资料库` 对应子目录 + 同名索引笔记
   - 知识笔记 → `03-笔记` 按主题归类
   - 项目内容 → `04-项目`
3. 归位后**删除原文**
4. 本目录内容**不写入正式索引**（INDEX.md）
````

#### 11. `02-资料库/AGENTS.md`

````text
# 02-资料库 — 原始材料区

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。

## 规则
1. 存放原始文件：证书、PDF、扫描件、图片等，**按主题分子目录**
2. 常见主题子目录（按需增删）：`奖项与荣誉`、`资格证书`、`学业材料`、`志愿活动`、`个人证件`、`工作材料`
3. 原始文件**只增不改**；需要更正时新建文件，不覆盖原件
4. 每个原始文件配一个同名（或同组）**.md 索引笔记**，写明：是什么 / 时间 / 来源 / 用途 / 存储路径
5. AI **优先读索引笔记**，而非直接解析二进制文件；确需解析时用 `06-Agent工具/` 中的工具
6. 含他人信息（姓名 / 学号 / 编号）或证件号的材料**仅本机使用，勿外发**（红线见根 AGENTS.md 第 6 节）
7. 新增材料后在 `INDEX.md` 的 `02-资料库` 章节登记
````

#### 12. `03-笔记/AGENTS.md`

````text
# 03-笔记 — 知识笔记区

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。

## 规则
1. 按主题分子目录（如 `学业研究` / `技能学习` / `生活记录`）；新主题先建目录再放笔记
2. 每篇笔记用 `00-模板/笔记模板.md` 初始化，必须带 YAML frontmatter
3. 修改笔记后更新 frontmatter 的 `updated`
4. 新增 / 移动笔记时同步更新 `INDEX.md` 对应章节
5. 内部链接用相对路径：`[标题](../03-笔记/主题/xxx.md)`
````

#### 13. `04-项目/AGENTS.md`

````text
# 04-项目 — 进行中的项目区

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。

## 规则
1. 每个项目一个子目录 `项目名/`，内含 `README.md`（目标 / 状态 / 进度 / 下一步）
2. frontmatter 的 `status` 表示阶段：`规划中` | `进行中` | `已完成` | `已归档`
3. 项目完成后：归档到 `03-笔记/` 对应主题，或在原目录标记 `已归档`，并更新 `INDEX.md`
4. 涉及研究数据、代码、敏感材料时遵守根 `AGENTS.md` 第 6 节隐私规则
````

#### 14. `05-作业/AGENTS.md`

````text
# 05-作业 — 作业区操作规范

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。
> 用途：AI 依据知识库协助完成作业。**核心规则：每个作业一个独立文件夹。**

## 1. 目录规则（硬性）

- **每个作业一个文件夹**，命名：`YYYYMMDD-课程名-作业名`
  - 例：`20260901-高等数学-第一章习题`、`20260905-工程图学-零件测绘作业`
- 一个作业文件夹内固定结构：

  ```
  <作业文件夹>/
  ├── README.md       # 作业元信息（必填，见 §2）
  ├── 题目/           # 原始题目：题目.pdf / 题目.md / 截图
  ├── 解答.md         # AI 完成的解答（必填）
  ├── 过程/           # 草稿、推导、多轮修改（可选）
  └── 附件/           # 数据、图片、代码等（可选）
  ```

- 新作业一律用 `_模板/` 中的模板初始化，**不要自创结构**
- **不要**在 `05-作业/` 根目录直接堆放作业文件

## 2. 作业元信息（README.md）

每份作业的 `README.md` 必须包含（模板见 `_模板/作业README模板.md`）：
- 课程、作业名、布置日期、**截止日期**、当前 `status`、评分 / 反馈
- `status` 取值：`未开始` | `进行中` | `待提交` | `已提交` | `已批改`

## 3. AI 协助作业流程（每次都要走）

1. **读题**：先读该作业文件夹的 `README.md` 与 `题目/`
2. **检索知识库**：在 `03-笔记/`、`02-资料库/` 中找相关笔记与资料；PDF / 图片必要时用 `06-Agent工具/` 中的工具解析
3. **作答**：写入该作业的 `解答.md`（带 frontmatter）
   - **标注来源**：引用知识库时用相对路径链接（如 `../../03-笔记/学业研究/xxx.md`）
   - **不编造**：不确定处标 `> 待确认：…`，不臆造公式 / 数据 / 结论
   - 保留推导与计算过程，便于核查
4. **更新状态**：同步更新 `README.md` 的 `status` 与 `updated`
5. **回灌知识库**：把可复用的知识点提炼成 `03-笔记/` 下的笔记，并与作业互链
6. **登记**：在根 `INDEX.md` 的 `05-作业` 章节登记

## 4. 其他

- Git 规则与隐私红线沿用根 `AGENTS.md` 第 5、6 节
- 作业完成后**不要删除**，作为记录保留；确需归档时把 `status` 更新为 `已批改`
````

#### 15. `05-作业/_模板/作业README模板.md`

````text
---
type: assignment
title: 
course: 
assigned: YYYY-MM-DD
deadline: 
status: 未开始
score: 
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# 作业名

## 要求摘要

（老师 / 课程的要求、评分标准、注意事项）

## 交付物清单

- [ ] `解答.md`
- [ ] 

## 进度记录

- YYYY-MM-DD 创建作业文件夹

## 反馈（批改后回填）

（得分、评语、错题）
````

#### 16. `05-作业/_模板/解答模板.md`

````text
---
type: assignment
title: 
course: 
assignment: 
deadline: 
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: 进行中
---

# 解答

## 题 1

**题干**：

**解**：

**依据**：（引用知识库笔记：`../../03-笔记/主题/xxx.md`）

---

## 题 2

**题干**：

**解**：

**依据**：
````

#### 17. `06-Agent工具/AGENTS.md`

````text
# 06-Agent工具 — 工具脚本与环境说明区

> 本指令在 AI 处理本目录内容时自动加载，优先级高于根 AGENTS.md。

## 用途
集中存放 AI 助手可用的**工具脚本、使用说明**，以及**本机环境说明**，供任何 AI 工具查阅。

## 建议文件
| 文件 | 内容 |
|---|---|
| `环境说明.md` | 本机环境清单：语言运行时、图像 / 文档工具、版本控制等（路径 + 可用库） |
| `工具用法.md` | 各工具调用方法：PDF 文本提取、页面渲染、图像 / 格式转换、Office 文档读取等 |
| `脚本/` | 可直接调用的工具脚本 |

## 规则
1. 新增工具时：脚本放 `脚本/`，用法补进 `工具用法.md`，环境变更补进 `环境说明.md`，并在根 `INDEX.md` 登记
2. 脚本升级**不覆盖旧版**：新建版本文件名或注明变更记录
3. 路径写**绝对路径**，便于跨目录直接调用
4. **不在此存放密钥、令牌、凭据**

## 提示
- 常见缺口：PDF 无文字层（扫描件）→ 先渲染成图片再识读；HEIC 等格式 → 先转换
- 任何解析结论都应**沉淀成 `02-资料库` 的索引笔记**，避免重复解析
````

### 完成后自检（把结果列出来）

- [ ] 17 个文件全部存在且非空
- [ ] 四个隐藏入口已创建：`.gitignore`、`.github/copilot-instructions.md`、`.cursor/rules/knowledge-base.mdc`、`.clinerules/knowledge-base.md`
- [ ] 7 个目录已创建：`00-模板` `01-收件箱` `02-资料库` `03-笔记` `04-项目` `05-作业` `06-Agent工具`
- [ ] `git log --oneline` 至少有 1 条提交（无 git 则说明）
- [ ] 向用户说明：以后把要整理的东西丢进 `01-收件箱`，然后让 AI "整理收件箱"

=== 复刻指令结束 ===

---

## 这份指令的设计取舍

| 取舍 | 原因 |
|---|---|
| **内联全部文件内容**（而不是"去 clone 某仓库"） | 自包含：任何人任何 agent、离线可用，不受仓库地址/网络/权限影响 |
| **明确要求"逐字照抄"** | 复刻的可信度来自规范文件本身；被 AI "润色" 会导致各人建出的库不一致 |
| **单列隐藏文件警告** | 实践中 `.gitignore`/`.github` 这类点文件最容易被漏建，而它们恰恰是跨工具兼容的关键 |
| **保留三条铁律与隐私红线** | 结构可以照抄，但规则不写清就会在第一次使用时走样 |
| **要求输出自检清单** | 让复刻结果可验证，而不是"看起来建好了" |

> 想装成可复用的 **skill**（而非一次性复刻），请改用仓库根目录的 `SKILL.md`，见 [`references/cross-agent.md`](references/cross-agent.md)。
